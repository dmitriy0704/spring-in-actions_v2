spring:
  application:
    name: tacocloud-auth-server
  datasource:
    url: jdbc:postgresql://localhost:5432/spring_in_actions?currentSchema=auth_server
    username: postgres
    password: dv9899
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  data:
    rest:
      base-path: /auth-api

server:
  port: 9000


